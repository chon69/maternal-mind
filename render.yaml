services:
  - type: web
    name: maternal-mind
    env: node
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_URL
        value: https://maternalmind.es
